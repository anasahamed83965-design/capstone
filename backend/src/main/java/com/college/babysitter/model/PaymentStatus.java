package com.college.babysitter.model;

/**
 * Sandbox payment states: PENDING (recorded, owed), PAID (settled),
 * REFUNDED (returned).
 */
public enum PaymentStatus {
    PENDING,
    PAID,
    REFUNDED
}
